const demoCard = "4400060111261741";

const checkIfCardValid = (cardNumber) => {
  let cardSum = 0;
  let isSecondDigit = false;

  for (let i = cardNumber.length - 1; i >= 0; i--) {
    if (isSecondDigit) {
    }
  }

  return cardSum % 10 === 0;
};
